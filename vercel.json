{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd dentis-site && npm install && npm run build",
  "outputDirectory": "dentis-site/dist",
  "installCommand": "echo 'install se face in buildCommand'",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
